"use strict";
// Copyright 2024 Fondazione Links
// SPDX-License-Identifier: Apache-2.0
Object.defineProperty(exports, "__esModule", { value: true });
exports.PQJwsVerifier = void 0;
const ml_dsa_1 = require("@noble/post-quantum/ml-dsa");
const _identity_wasm_1 = require("./identity_wasm.js");
class PQJwsVerifier {
    verify(alg, signingInput, decodedSignature, publicKey) {
        let res = false;
        let ctx = undefined;
        if (alg !== "ML-DSA-44" /* JwsAlgorithm.MLDSA44 */
            && alg !== "ML-DSA-65" /* JwsAlgorithm.MLDSA65 */
            && alg !== "ML-DSA-87" /* JwsAlgorithm.MLDSA87 */
            && alg !== "id-MLDSA44-Ed25519" /* JwsAlgorithm.IdMldsa44Ed25519 */
            && alg !== "id-MLDSA65-Ed25519" /* JwsAlgorithm.IdMldsa65Ed25519 */) {
            throw new Error("unsupported JWS algorithm");
        }
        const pubKey = decodeJwk(publicKey);
        // Domain separator for hybrid signatures
        if (alg === "id-MLDSA44-Ed25519" /* JwsAlgorithm.IdMldsa44Ed25519 */) {
            ctx = Uint8Array.from(Buffer.from("060B6086480186FA6B5008013E" /* CompositeAlgIdDomain.IdMldsa44Ed25519 */, "hex"));
        }
        else if (alg === "id-MLDSA65-Ed25519" /* JwsAlgorithm.IdMldsa65Ed25519 */) {
            ctx = Uint8Array.from(Buffer.from("060B6086480186FA6B50080147" /* CompositeAlgIdDomain.IdMldsa65Ed25519 */, "hex"));
        }
        if (alg === "ML-DSA-44" /* JwsAlgorithm.MLDSA44 */ || alg === "id-MLDSA44-Ed25519" /* JwsAlgorithm.IdMldsa44Ed25519 */) {
            res = ml_dsa_1.ml_dsa44.verify(pubKey, signingInput, decodedSignature, ctx);
        }
        else if (alg === "ML-DSA-65" /* JwsAlgorithm.MLDSA65 */ || alg === "id-MLDSA65-Ed25519" /* JwsAlgorithm.IdMldsa65Ed25519 */) {
            res = ml_dsa_1.ml_dsa65.verify(pubKey, signingInput, decodedSignature, ctx);
        }
        else if (alg === "ML-DSA-87" /* JwsAlgorithm.MLDSA87 */) {
            res = ml_dsa_1.ml_dsa87.verify(pubKey, signingInput, decodedSignature);
        }
        if (!res) {
            throw new Error("signature verification failed");
        }
    }
}
exports.PQJwsVerifier = PQJwsVerifier;
function decodeJwk(jwk) {
    if (jwk.alg() !== "ML-DSA-44" /* JwsAlgorithm.MLDSA44 */ && jwk.alg() !== "ML-DSA-65" /* JwsAlgorithm.MLDSA65 */
        && jwk.alg() !== "ML-DSA-87" /* JwsAlgorithm.MLDSA87 */) {
        throw new Error("unsupported `alg`");
    }
    const paramsPQ = jwk.paramsAkp();
    if (paramsPQ) {
        let textEncoder = new TextEncoder();
        return (0, _identity_wasm_1.decodeB64)(textEncoder.encode(paramsPQ.pub));
    }
    else {
        throw new Error("expected Okp params");
    }
}
//# sourceMappingURL=pq_verifier.js.map