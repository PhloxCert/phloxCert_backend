import { IJwsVerifier, Jwk } from "./identity_wasm.js";
import { JwsAlgorithm } from "./jose";
export declare class PQJwsVerifier implements IJwsVerifier {
    verify(alg: JwsAlgorithm, signingInput: Uint8Array, decodedSignature: Uint8Array, publicKey: Jwk): void;
}
